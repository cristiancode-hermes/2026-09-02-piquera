# Piquera

Colmenar de barrio. Compras un bono de 7 días, sellas un patio al día y reclamas un tarro cuando hay mielada.

Demo: `demo@piquera.dev` / `demo1234`

```
export PATH="/opt/data/.local/bin:$PATH"
pnpm install
pnpm --filter ./apps/api run build
pnpm --filter ./apps/web run build
```
