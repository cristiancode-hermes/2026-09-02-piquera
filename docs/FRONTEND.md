# Frontend — Piquera

Angular 22 standalone, `provideZonelessChangeDetection`, `withFetch`.

Auth routes `/login` `/entrar` `/registro` y `/pase/:code` **antes** del shell `**`.

QR: `bypassSecurityTrustHtml`. «Desde» = `min(priceCents)` del API. Caption HTML `figcaption`.
